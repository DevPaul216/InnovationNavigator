{
  "name": "innovationgroup",
  "schema": {
    "type": "object",
    "properties": {
      "InnovationIssue": {
        "type": "array",
        "description": "A list of problem, issues or challenges that need to be solved",
        "items": {
          "type": "string",
          "description": "A single problem, issue or challange that needs to be solved."
        }
      },
      "TargetGroup": {
        "type": "array",
        "description": "A list of differnt people or groups affected by the issue.",
        "items": {
          "type": "string",
          "description": "A single person, group or demografic that is affected by the issue."
        }
      }
    },
    "required": [
      "InnovationIssue",
      "TargetGroup"
    ],
    "additionalProperties": false
  },
  "strict": true
}
